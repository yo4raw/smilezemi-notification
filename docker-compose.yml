services:
  crawler:
    build:
      context: .
      dockerfile: Dockerfile
    # 環境変数は .env からそのまま注入する
    env_file:
      - .env
    volumes:
      # スクリーンショット保存
      - ./screenshots:/app/screenshots
      # デバッグ用: ソースコードとスクリプト
      - ./src:/app/src
      - ./scripts:/app/scripts
